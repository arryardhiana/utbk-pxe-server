services:
  dhcp-server:
    image: alpine:latest
    container_name: pxe-dhcp
    network_mode: host
    restart: "no"
    cap_add:
      - NET_ADMIN
    volumes:
      - ./scripts/dnsmasq.conf:/etc/dnsmasq.conf:ro
    command: /bin/sh -c "apk add --no-cache dnsmasq && dnsmasq -k"
